spring.application.name=apigateway

jwt.secret=testsecretkeytestsecretkeytestsecret12
jwt.expiration=86400000

gateway.secret=test-gateway-secret

# FIX: Added internal.secret — was missing from test properties, causing
# @Value("${internal.secret}") to fail if any bean references it
internal.secret=test-internal-secret

services.auth.url=http://localhost:8081
services.project.url=http://localhost:8082

# FIX: Added task service URL to match GatewayRoutesConfig @Value("${services.task.url}")
services.task.url=http://localhost:8083